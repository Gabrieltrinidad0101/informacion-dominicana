        delete from payrolls where
                date = ${date} and
                institution_name = ${institutionName} and
                index = ${index} and
                trace_id = ${traceId} and 
                link = ${link}